on:
  push:
    branches:
      - master

name: Run shelscript

jobs:
  shelscript:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: run-shel
        uses: actions/checkout@v2
        run: sh hello.sh